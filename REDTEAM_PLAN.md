# 步道乐跑反作弊红队测试方案

> 状态：方案设计（待评审）
> 范围：仅限步道乐跑团队**授权测试环境 / 沙箱测试账号**内执行
> 原则：攻击 PoC 仅用于验证检测召回率，禁止外传为通用刷分工具

---

## 1. 文档目的与范围

通过**红队（Red Team）模拟攻击**的方式，复现当前黑产/开源社区对已上线跑步打卡系统的攻击手法，
驱动反作弊检测能力的建设，最终形成"攻击复现 → 检测拦截 → 指标验证 → 规则调优"的闭环。

- **目标系统**：步道乐跑客户端 + 上报/校验服务端（自有系统）
- **不在范围内**：任何对第三方系统、非授权账号、真实学生成绩的操纵

---

## 2. 术语

| 术语 | 含义 |
|---|---|
| PoC | Proof of Concept，用于验证漏洞/检测可达性的最小复现 |
| 红队 | 以攻击视角发现系统弱点，服务于防御加固 |
| 设备指纹 | 基于硬件/系统特征生成的稳定设备标识 |
| 联合一致性 | 多源信号（GPS/IMU/气压/网络）间的物理相关性校验 |
| 召回率 | 攻击样本被检测命中的比例 |
| 误报率 | 真实学生轨迹被误判为异常的比例 |

---

## 3. 总体架构

```
┌─────────────┐   攻击流量    ┌──────────────────┐   埋点上报   ┌──────────────┐
│ 红队PoC环境 │ ───────────▶ │ 步道乐跑客户端   │ ─────────▶ │ 检测服务端   │
│(沙箱/模拟器)│              │ (采集层埋点)     │             │              │
└─────────────┘              └──────────────────┘             │ ├ 实时规则   │
                                                              │ ├ 离线ML    │
                                                              │ └ 风险评分  │
                                                              └──────┬───────┘
                                                                     │ 结果回流
                                                              ┌──────▼───────┐
                                                              │ 红队分析看板 │
                                                              │ 召回/误报统计│
                                                              └──────────────┘
```

### 3.1 计划目录结构（待开发阶段落地）

```
budaolepao/
├── docs/
│   └── REDTEAM_PLAN.md            # 本文件
├── poc/                           # 攻击复现（仅沙箱）
│   ├── emulator_run/              # 参考开源：模拟器+定位注入轨迹
│   └── README.md                  # 明确标注"仅授权测试"
├── detection/                     # 检测侧（核心交付）
│   ├── client/                    # 客户端环境指纹采集 (Kotlin)
│   ├── server/                    # 轨迹异常检测 (Python)
│   └── rules/                     # 规则引擎配置
└── eval/                          # 验证闭环
    ├── baselines/                 # 真实学生轨迹基线
    └── metrics.py                 # 召回率/误报率统计
```

---

## 4. 威胁建模（攻击向量）

| # | 向量 | 描述 | 开源社区参考 |
|---|---|---|---|
| V1 | 虚拟定位 | Mock Location / Fake Location 注入 GPS | hyper152/happy_run |
| V2 | 模拟器/虚拟机 | MuMu / 蓝叠 / 光速虚拟机跑客户端 | ranfey/RunInMumu, EGO-tlm |
| V3 | 轨迹伪造 | 脚本插值广播 GPS 坐标（无真实位移） | ranfey/RunInMumu |
| V4 | 传感器缺口 | 仅改 GPS，步频/IMU 缺失或不一致 | 现有开源脚本通病 |
| V5 | 多账号规模化 | 同机多开 / 同 IP 集群刷取 | achenc1013/Run_Script_Plus |
| V6 | 接口逆向 | 抓包重放，绕过 APP 直接推数据 | 逆向类仓库 |

---

## 5. 攻击 PoC 复现（沙箱）

> 仅用于在自有测试账号上跑通攻击链，验证下列检测是否触发。

### 5.1 电脑端模拟器链（V1+V2+V3+V5）
参考 `ranfey/RunInMumu` 的技术链路：
1. Windows + 安卓模拟器，实例内安装客户端 + 登录方式
2. 定位注入：模拟器虚拟定位 或 `MuMuManager control -v N tool location`
3. 轨迹生成：经纬度列表插值 + 速度/抖动参数周期性广播
4. UI 驱动：ADB 截图 + 模板匹配点击；或直接坐标点击
5. 多账号：模拟器多开 N 实例，外层循环 `-v 0..N-1`

### 5.2 其他向量
- V4：使用仅 GPS 注入、关闭传感器模拟的 PoC 配置
- V6：抓包重放上传请求，验证服务端是否校验运行时环境证明

---

## 6. 检测架构设计（核心交付）

### 6.1 采集层（客户端埋点）
上报除 GPS 外的多维信号，供服务端联合校验：
`GPS / 步频 / 加速度 / 陀螺仪 / 气压高度 / 网络定位 / 基站 / 环境指纹 / 行为序列时间戳`

### 6.2 实时规则引擎（命中即拒）
| 规则 | 阈值示例 | 对应向量 |
|---|---|---|
| Mock 标记 | `Location` 携带 mock 标记 | V1 |
| 精度异常稳定 | 连续点 accuracy 方差 < ε | V1 |
| 模拟器指纹 | `Build`/传感器数命中指纹库 | V2 |
| 速度生理上限 | 瞬时 > 12 m/s 或加速度 > 10 m/s² | V3 |
| 噪声谱 | 自相关检验非 1/f 相关 | V3 |
| 轨迹自相似 | 同账号路线 Jaccard > 0.9 | V3/V5 |
| 传感器一致性 | GPS 位移与步频零相关 | V4 |
| 多账号 | 同设备指纹关联账号 > 阈值 | V5 |
| 请求完整性 | 缺运行时环境证明/签名 | V6 |

### 6.3 离线异常模型（ML）
- **轨迹聚类**：同校区路线相似度聚类，离群/重复打标
- **行为离群**：配速分布、上传时间规律、设备-账号图聚类
- **传感器一致性模型**：GPS×步频×IMU×气压 联合相关性打分

### 6.4 风险评分
规则命中 + 模型评分 → 风险分；超阈值标记"行为不规范"并进入人工复核队列，
避免直接封禁导致误伤。

---

## 7. 验证闭环

### 7.1 流程
```
构造PoC(各向量) → 跑检测 → 统计命中 → 用真实基线校准误报 → 调阈值 → 回归
```

### 7.2 指标
- **召回率**：每种攻击向量 PoC 被命中的比例（目标趋近 100%）
- **误报率**：真实学生轨迹被误判比例（须用真实基线校准，目标极低）

### 7.3 基线
- 收集真实学生轨迹作为 `eval/baselines/`，所有阈值调优以不误伤真实用户为前提

---

## 8. 开发里程碑（建议）

| 阶段 | 内容 | 交付 |
|---|---|---|
| M1 | 检测服务端骨架 + 规则引擎 | `detection/server`, `detection/rules` |
| M2 | 客户端环境指纹采集 | `detection/client` |
| M3 | 攻击 PoC 沙箱复现 | `poc/`（标注仅授权测试） |
| M4 | 离线异常模型 + 评估看板 | `eval/metrics.py` |
| M5 | 召回/误报回归与调优 | 报告 |

---

## 9. 合规边界

- 所有 PoC 仅在步道乐跑团队**自有授权测试账号 / 沙箱**执行
- `poc/` 目录须含醒目声明，禁止打包分发为通用刷分工具
- 真实学生数据仅用于误报率基线校准，须脱敏
- 本方案不产出"规避检测的调参技巧"，只产出"检测与拦截能力"

---

_评审通过后进入开发阶段（M1 起）。_
